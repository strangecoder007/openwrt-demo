// 服务器地址不再让用户填写：这台家庭 NAS 的入口是固定的，写死在这里。
// 以后换域名 / 换端口只改这一处，登录页与注册页都从这里取。
//
// 注意：不要把这个值回填进表单让用户改 —— 用户改了会写进 davSession，
// 下次登录页拿不到输入框就会一直用那个错误地址，且没有任何入口能改回来。
module.exports = {
  BASE_URL: 'https://cy.gcaiyy.xyz:34443'
};
